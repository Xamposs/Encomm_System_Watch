# ENCOMM SYSTEM WATCH — Development Roadmap

Status legend:

| State | Meaning |
|---|---|
| `NOT STARTED` | no implementation work yet |
| `IN PROGRESS` | partial implementation, not verifiable end-to-end |
| `FUNCTIONAL` | works with real data, polish/edge cases remain |
| `COMPLETE` | implemented, tested, verified on the live machine |

---

| Phase | Scope | Status |
|---|---|---|
| **01** | Project Foundation — repo, backend/frontend scaffold, launcher, docs, CI-ready git workflow | **COMPLETE** |
| **02** | Windows Process Collector — stable IDs, defensive field collection, CPU/RAM, metadata caching | **COMPLETE** (30 backend tests; 0.6 s steady tick on 295 processes) |
| **03** | Live Process Graph — processes as nodes, compact cards, fcose layout, incremental updates | **COMPLETE** (verified in acceptance: 300+ process nodes) |
| **04** | Real Network Connections — TCP/UDP sockets, listening/localhost/external classification, proc↔proc pairing, endpoint nodes with caps | **COMPLETE** (verified: 60 localhost edges, 54+ external edges) |
| **05** | WebSocket Event Streaming — snapshot on connect, event batches, stats heartbeat, auto-reconnect | **COMPLETE** (verified: reconnect after refresh, Test J) |
| **06** | Animated Live Connections — traveling-signal canvas overlay, open/close/update pulses, recent-activity flow, graceful fades | **COMPLETE** (verified: recent edges + CONNECTION OPENED events, Test F) |
| **07** | System Metrics — machine CPU/RAM header stats, per-process CPU/RAM, throttled metric events | **COMPLETE** (verified: METRICS events flowing, Test G) |
| **08** | Windows Services — service enumeration (name, state, start type, account) as nodes | **COMPLETE** (0.4.0: psutil win_service_iter — READ-ONLY; name/display/status/start-type/account/binpath/description/PID; TTL-cached metadata; per-service degradation; N services in one svchost → N HOSTED_BY edges to the ONE real process (never a fake process per service); compact ⚙ SERVICE nodes; change-only SERVICE_STARTED/STOPPED/STATUS_CHANGED; verified LIVE: 293 services, 119 PID mappings, shared host BFE+mpssvc→pid 3624) |
| **09** | Docker + WSL observability | **COMPLETE** (0.4.0, capability-aware — see Phase 21 note below for the same rule applied to VMs): WSL read-only discovery (`--list --verbose/--running/--status`, UTF-16-resilient), STOPPED distros never started/inspected, bounded internal summaries for RUNNING distros only; Docker via local engine CLI/npipe (never TCP 2375): engine/version/containers/states/ports/networks, targeted `inspect --format` PID only, NO container ENV/labels serialization; ⬡ WSL / ◆ DOCKER ENGINE / ◇ CONTAINER / NET nodes, HOSTS/EXPOSES/CONNECTED_TO edges, change-only CONTAINER_* events. LIVE: WSL Ubuntu+docker-desktop discovered (both stopped, summary SKIPPED); Docker client installed, engine NOT running → truthful SKIPPED (fixture-tested) |
| **10** | Advanced Node Inspector — read-only details panel, no controls | **COMPLETE** (added NETWORK section: ↓/↑ rates, connections, last activity — only when real telemetry exists; 0.3.0 adds SEMANTIC IDENTITY + GPU sections; 0.4.0 adds WINDOWS SERVICE / WSL DISTRIBUTION / DOCKER ENGINE / CONTAINER / DOCKER NETWORK / VIRTUAL MACHINE sections) |
| **11** | Search / Filtering / Grouping — search highlight, ALL/ACTIVE/LISTENING/HIGH CPU filters, family grouping | **COMPLETE** (search + 4 filters + NODES/FAMILIES toggle using real parent_sid evidence; verified Tests N, P) |
| **12** | Live Event Drawer — bounded chronological feed | **COMPLETE** (800-event buffer, verified Test I; 0.3.0 adds semantic event rows: HERMES/LM STUDIO/MCP DETECTED, MODEL LOADED/AVAILABLE, GPU PROCESS ATTACHED/DETACHED, SEMANTIC LOST) |
| **13** | Visual Polish — control-room theme, zoom-aware labels, layout tuning | **COMPLETE** (near-black micro-grid canvas, wireframe far-zoom mode, semantic label buckets, brightened palette, edge activity styling, focus dimming, live 1600×1000 screenshot verified visually) |
| **14** | LM Studio Detection — local LLM API discovery (localhost sockets + HTTP probe) | **COMPLETE** (0.3.0: evidence-based detector — real socket-topology port discovery, loopback-only `/v1/models` + `/api/0/models` probes (throttled/cached), strict LOADED (runtime-proven) vs AVAILABLE distinction, model nodes + SERVES_MODEL/LOCAL_API/HOSTS edges, CONFIRMED only via known path or live API; unit-tested incl. false-positive rejection, API-failure degradation and timeout isolation) |
| **15** | Hermes Detection — Hermes gateway/agent process identification | **COMPLETE** (0.3.0: evidence-combination detector — desktop app exact-path identity (CONFIRMED), `hermes_cli.main … serve` gateway detection, Electron/gateway family with identity-gated ancestry (no session-tree absorption), MEMBER_OF + real PROCESS_PARENT + HOSTS relationships; verified LIVE on this machine: 9 real Hermes processes, CONFIRMED, zero false positives) |
| **16** | MCP Server Detection — MCP server process/socket mapping | **COMPLETE** (0.3.0: stdio/HTTP/SSE — anchored cmdline tokens, package paths, parent/child ancestry (Hermes gateway launcher), owned listeners; identity proven → named (`filesystem`, `github`, …), proven-but-unidentified → `unknown`; SPAWNED/PROCESS_PARENT for stdio (never fake network edges); unit-tested: ancestry, stdio, HTTP, false-positive rejection; acceptance: REAL or SKIPPED with fixture tests labeled separately) |
| **17** | AI Agent Telemetry — agent nodes, tool calls, agent-to-agent messages, tokens/TPS/latency | **FUNCTIONAL** (0.5.0 — REAL application-level telemetry pipeline; real producer integration pending for deep metrics): normalized `AITelemetryEvent` schema (every metric optional, absent when no real source), privacy gate (prompt/response/reasoning/credential content rejected), failure-isolated providers with truthful states ACTIVE/AVAILABLE_NO_DATA/UNAVAILABLE/DEGRADED + per-metric availability matrix, bounded buffer (500 history/20 traces/100 spans/600 s TTL) with real trace correlation, **REAL Hermes gateway adapter** (discovers `hermes_cli.main … serve` processes + localhost listeners, polls unauthenticated `/api/status` + `/api/health`, AGENT_RUN_STARTED/FINISHED from real count deltas, AI_ERROR change-only; verified LIVE: 2 real gateways, ACTIVE, 1 session — tokens/TPS/tool names truthfully UNAVAILABLE, 401-protected/not exposed), OTEL seam (READY / NO REAL PRODUCER), env-gated TEST/FIXTURE provider (`ESW_AI_TELEMETRY_FIXTURE=1`, every event test_only, never mixes with real mode), `POST /api/ai-telemetry/events` localhost-only bounded metadata ingestion (OBSERVE ONLY — no control paths, security-tested), `GET /api/ai-telemetry`, WS `ai_activity`/`ai_metrics`/`ai_provider_status` (change-only, benchmark-suppressed), bounded transient AI_RUNTIME nodes (cap 24, TTL decay) + AI_CALL edges on proven evidence + distinct fuchsia AI signal lane (own budgets), inspector AI TELEMETRY section, drawer AI rows, header chips only with real data; 37 new backend tests (262 total) + acceptance AC0–AC29; status: **FUNCTIONAL — real producer integration pending** (deep token/TPS/tool telemetry requires a safe authenticated interface) |
| **18** | GPU / VRAM Telemetry — nvidia-smi / NVML collectors | **COMPLETE** (0.3.0: NVML primary (`nvidia-ml-py`/pynvml, no subprocess), nvidia-smi CSV fallback, multi-GPU `gpu:<index>` resource nodes, ~1 s metrics / ~2 s PID attribution, per-process VRAM only when NVML provides it, change-only GPU_PROCESS_ATTACHED/DETACHED, USES_GPU edges; verified LIVE: GTX 1660 Ti — real name/util/VRAM/temp/power/driver + 20 attributed PIDs; failure-isolated) |
| **19** | Advanced Network Activity — per-connection byte/rate telemetry with honest capability tiers | **COMPLETE** (0.2.3: verified with REAL elevated Microsoft-Windows-TCPIP ETW — not synthetic telemetry. Modern Windows 11 manifest support: Tcb correlation (rundown/connect/accept → TCB cache → TcpDataTransferSend/Receive lookup → PID + real 4-tuple + direction + bytes), UDP self-contained events, readiness INITIALIZING/ACTIVE/DEGRADED; wildcard local-IP fallback (0.0.0.0/::) for outbound ETW attribution with ambiguity-safe single-candidate lookup + diagnostic counters; 0.2.3 adds topology-driven TCB cleanup (`drop_tcb_tuples` — closed connections leave the correlation map ~1-2 s after the psutil snapshot sees them go, closing the kernel Tcb-handle-recycling misattribution race measured at 18k unattributed events); `tools/verify_tier2.ps1` 10/10 with real directional bytes; acceptance 76/76 (Tests R/S/T) with REAL DATA particles on real edges; harness PROCESS↔PROCESS edge measured 445 MB fwd / 90 MB rev of real bytes; 101 backend tests. Wildcard branch: unit-tested, not observed in the runtime window) |
| **20** | Large Graph Optimization — 1000+ node validation, clustering, virtualization | **COMPLETE** (0.3.1: TEST-ONLY deterministic benchmark mode validated 500/1000/1500/2000-node fixtures; incremental updates + label cache; size-scaled layout policy + manual RELAYOUT; far-zoom edge LOD; bounded prioritized particles 140/400/2-4-6; AI toggle + search/filter measured (5-18 ms at 500-1500 nodes); PerfMonitor diagnostics; read-only ETW attribution health detector; 30-min long-run bounded; fixed latent filter-restore bug (display:none style pass); real graph regression green) |
| **21** | Virtual Machine Observability — Hyper-V / VMware / VirtualBox read-only detection | **COMPLETE** (0.4.0: generic VM detector framework — Hyper-V via read-only CIM `Get-VM` + vmwp.exe GUID→PID host mapping, VMware via vmware-vmx.exe + .vmx evidence + read-only `vmrun list`, VirtualBox via `VBoxManage list runningvms` only, generic hypervisor-process fallback; identity evidence-backed (unproven → `VIRTUALIZATION PROCESS`, never a made-up name); VM paths sanitized; VM nodes + HOSTS/BACKED_BY edges, USES_GPU only when NVML proves the VM HOST process uses the GPU; change-only VM_DETECTED/LOST/STATE_CHANGED; no control commands ever (security-test-scanned). REAL validation on this machine: all three hypervisors installed but ZERO running VMs → truthful `REAL VM VALIDATION: SKIPPED — NO RUNNING VM` (fixtures cover every provider; never auto-started) |
| **22** | Final UI Fidelity / Control-Room Visual Pass | **COMPLETE** (0.6.0: fullscreen graph-first shell — canvas owns ~85 % of the viewport; compact ~34 px header with SYSTEM/AI/INFRA tabs + `LIVE · READ ONLY`; all node kinds scaled ~30 % smaller (PROCESS 108×38, SYSTEM 150×40, SERVICE 112×36); hairline 1 px curved wiring via deterministic per-edge `unbundled-bezier` control points (low idle opacity, bright only under real activity); fcose density retune (repulsion 14000 / edge length 125 / gravity 0.10) → 800 real nodes fit at once with 0 card overlaps; initial fit clamped to a 0.55 zoom floor so the opening view is a readable dense overview; per-bucket labels (FAR none / MID name+PID / NEAR +CPU%+MEM) with subtle dark text backing over wiring; legend collapsed to a chip; 18 px bottom hint bar above the 28 px LIVE EVENT DRAWER strip; flat near-black canvas + faint micro-grid; explicit `label: data(label)` fix for the cytoscape 3.34 default-mapping omission; 3+ screenshot comparison iterations vs the supplied reference on the REAL graph; new acceptance AD section (13 objective shell checks; canvas ≥ 72 % viewport height floor); real-data screenshot updated; performance non-regression (same budgets, idle rAF stop, LOD); backend untouched — 0 regressions, 0 fabricated telemetry) |
| **23** | v1.0 Release / Packaging — production install + start workflow, release documentation, tagging | **COMPLETE** (1.0.0, 2026-08-20: `Setup-SystemWatch.ps1` one-time bootstrap — venv + backend deps + `npm ci` + production build, never touches system software, never auto-elevates; `Start-SystemWatch.ps1` canonical production launcher — project-root resolution (space-safe), venv/build verification with clear errors, single-instance port check (healthy existing instance reported, foreign listener fails safely, never kills), stale `esw-telemetry` ETW session warning (detected, cleanup command printed, never stopped automatically), no auto-elevation (Administrator state reported truthfully with TIER0/limited explanation for non-admin), child output captured to `backend\esw-backend.log`, live capability banner (`Mode: LIVE`, `ETW: TIER2 / ACTIVE`, `Read-only: YES`), optional `-NoBrowser`, cleans up only its own processes; `Start-SystemWatch.bat` convenience wrapper; version 1.0.0 in FastAPI app + package.json + package-lock.json; README quick start + Windows-11-primary statement + optional-capabilities matrix + expanded limitations; `docs/RELEASE_1.0.0.md`; CHANGELOG [1.0.0]; consistency audit (viewport claims unified at ~85 % descriptive / ≥72 % acceptance floor; benchmark + AI fixture TEST-ONLY opt-in, release starts REAL); new acceptance AE release checks; validation: backend 262/0, frontend typecheck + build PASS, acceptance 213/0 + AE, launcher verified from a spaces-containing path; tagged `v1.0.0`) |

---

## Phase checkpoint rule

Every future phase ends with:

```
IMPLEMENT → TEST → UPDATE DOCS → UPDATE CHANGELOG → GIT STATUS → COMMIT
→ PUSH TO main → VERIFY PUSH → REPORT COMMIT SHA
```
